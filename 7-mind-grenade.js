const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is: ${num1 + num2}`);
}

addValues() // if app is executed on the module NOT EXPORTED then if its invoked on main app it will display